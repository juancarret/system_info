# system_info
How can we use 'platform' in python to get some data from systems like OS version, CPU data, release version and more. The main .py is systeminfo.py, system.py is where methods are contained, and there is a 'systeminfo.exe' in the file named 'dist', it was generated with 'pyinstaller', is so easy to use.
